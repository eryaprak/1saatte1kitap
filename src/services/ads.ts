// Stub: google-mobile-ads kaldırıldı, premium app — reklam yok
export const initializeAds = () => Promise.resolve();
export const showInterstitial = () => Promise.resolve();
export const showRewarded = () => Promise.resolve();

// Eski fonksiyon adı için alias (backward compat)
export const showInterstitialSequence = showInterstitial;
